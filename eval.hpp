#ifndef EVAL_HPP
#define EVAL_HPP

#include <vector>
#include <string>
#include <stack>
#include <regex>
#include <iostream>

#include <boost/lexical_cast.hpp>

#include "types.hpp"

std::string evaluate(const std::vector<std::string> &in);

#endif // EVAL_HPP
